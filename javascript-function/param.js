function whichIsLonger(first, second) {
    if(first.length>second.length){
        return first
    }
    else if(first.length===second.length){
        return first
    }
    return second
   }
   
   whichIsLonger('nitu', 'mish');
